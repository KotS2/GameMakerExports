/// Object : obj_speechbubble | Step_0




timer--;
if (timer <= 0) instance_destroy();

// Eğer player’a bağlı kalsın istiyorsan:
if (parent_id != noone && instance_exists(parent_id)) {
    x = parent_id.x;
    y = parent_id.y - 64;
}
"
// obj_speechbubble Step
timer--;
if (timer <= 0) instance_destroy();

// Eğer player’a bağlı kalsın istiyorsan:
if (parent_id != noone && instance_exists(parent_id)) {
    x = parent_id.x;
    y = parent_id.y - 64;
}